what you may want to check out:

rReg.sql: creation of many tmp tables before getting the final big table with player features.

rReg.py: run this one will create regTable.sql which is the sql to insert data into the regTable (big table with player features).

nba.db: this db here is updated with many more tmp tables added.
